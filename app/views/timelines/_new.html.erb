<h2>New Plurk</h2>
<% @plurk=Plurk.new %>
<%= form_for(@plurk) do |f| %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
